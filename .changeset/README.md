# Changesets

This folder stores individual changesets. Each file represents a change that should be included in the next release.

To add a changeset, run:
```bash
npm exec changeset
```

This will create a new markdown file in this directory describing your changes.
